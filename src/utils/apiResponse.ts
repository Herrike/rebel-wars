import {State as ApiResponseState} from '../hooks/useFetch'
// export const getApiResponse = (responseState: ApiResponseState<unknown>): {
//     data?: Record<string, unknown>
//     error?: Error
// } => {
//     if(responseState.state === 'fetched' && data instanceof Object) {
//         return {data}
//     }
// }